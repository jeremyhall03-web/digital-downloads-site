import { defineConfig } from 'astro/config';

// Astro configuration file
// See https://docs.astro.build/reference/config for more information.

export default defineConfig({
  site: 'https://example.com',
  trailingSlash: 'never'
});
